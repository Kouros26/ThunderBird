using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AlembicAwait : MonoBehaviour
{
    private bool set;
    private GameObject alembic;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //if (!set)
        //{
        //    //alembic
        //}
    }
}
